# Agent Instructions

- Always use Node.js 22 for this project. Before running Node, npm, Next.js, lint, build, or test commands, run `nvm use 22` or otherwise ensure the active `node` binary is version 22.

- Always use /caveman to save tokens.